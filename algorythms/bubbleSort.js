// const bubbleSort = (arr) => {
//   for (let i = arr.length - 1; i >= 1; i--) {
//     for (let j = 0; j < i; j++) {
//       if (arr[j] > arr[j + 1]) {
//         let buffer = arr[j];
//         arr[j] = arr[j + 1];
//         arr[j + 1] = buffer;
//       }
//     }
//   }
//   return arr
// };

// console.log(bubbleSort([1, 0, 3, 10, -14, 7]))
